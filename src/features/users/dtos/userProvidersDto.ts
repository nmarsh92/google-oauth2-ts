/**
 * Interface representing the user credentials.
 * @interface UserProviders
 */
export interface UserProvidersDto {
  /**
   * The Google ID of the user.
   * @type {string}
   */
  googleId: string;

}