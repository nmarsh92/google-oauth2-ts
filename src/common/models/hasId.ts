/**
 * Interface representing an object with an 'id' property.
 * @interface HasId
 */
export interface HasId {
  /**
   * The unique identifier for the object.
   * @type {string}
   */
  id: string;
}